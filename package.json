{
  "name": "my-static-portfolio",
  "version": "0.0.1",
  "homepage": "https://Recvi.github.io",
  "private": true,
  "dependencies": {
    "next": "latest",
    "react": "latest",
    "react-dom": "latest",
    "serve": "latest"
  },
  "devDependencies": {
    "gh-pages": "latest"
  },
  "scripts": {
    "dev": "NODE_OPTIONS='--inspect' next dev",
    "build": "next build",
    "preexport": "npm run build",
    "export": "next export",
    "predeploy": "npm run build",
    "deploy": "gh-pages -b master -d build",
    "prestart": "npm run export",
    "start": "serve out"
  }
}
